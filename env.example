# Server Configuration
PORT=3000
NODE_ENV=development

# Database - Option 1: Use connection URL
DATABASE_URL="postgresql://postgres:password@localhost:5432/mydb?schema=public"

# Database - Option 2: Use individual parameters (alternative to DATABASE_URL)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=password
# DB_NAME=mydb

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS (comma-separated for multiple origins, e.g. web + Expo)
FRONTEND_URL=http://localhost:5173

# Real-time socket (set to 0 or false to disable)
SOCKET_ENABLED=1

# Email Configuration (for password reset)
# Option 1: Use Brevo (Sendinblue) - Recommended
# Get your SMTP credentials from: https://app.brevo.com/settings/keys/smtp
BREVO_SMTP_KEY=your-brevo-smtp-key-here
BREVO_SMTP_LOGIN=your-login@smtp-brevo.com
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=587
BREVO_SMTP_SECURE=false
BREVO_SMTP_FROM=noreply@yourdomain.com

# Option 2: Use Generic SMTP (Gmail, SendGrid, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@schoolhub.com

# Fallback: Generic email from address (used if not specified above)
EMAIL_FROM=noreply@schoolhub.com

# Note: For development, emails will be logged to console if no email service is configured

# Note: API routes are now at root level (not /api)
# Example: http://localhost:3000/auth/login
